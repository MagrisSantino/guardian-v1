# REGLAS DEL PROYECTO GUARDIAN
Eres un desarrollador experto en Next.js, Tailwind CSS y Supabase.

## ESQUEMA DE LA BASE DE DATOS (SUPABASE)
ESTRICTO: Nunca inventes nombres de columnas. Usa ÚNICAMENTE las columnas listadas a continuación para cada tabla.

### 1. Tabla: `profiles` (Perfiles de usuarios)
- `id` (uuid, primary key)
- `full_name` (text) - Nombre del médico o Razón Social del prestador
- `admin_name` (text) - Nombre del administrador (Solo prestador)
- `role` (text: 'doctor', 'clinic_admin', 'super_admin')
- `is_verified` (boolean, default false)
- `dni` (text, UNIQUE)
- `matricula` (text, UNIQUE)
- `birth_date` (date) - Fecha de nacimiento
- `cuit` (text) - CUIT/CUIL del prestador
- `specialty` (text)
- `avatar_url` (text) - Foto de perfil / Logo prestador
- `cover_url` (text) - Foto de portada
- `bio` (text)
- `experience_tags` (text[])
- `whatsapp` (text) - Número de contacto unificado
- `prestador_type` (text)
- `location_maps` (text) - Dirección física
- `km_from_cba` (integer)
- `complexity` (text[])
- `resources` (text[])
- `services` (text[])
- `blocked_dates` (text[])
- `rating` (numeric)
- `reviews_count` (integer)
- `created_at` (timestamp)
- `updated_at` (timestamp)

### 2. Tabla: `shifts` (Guardias u Ofertas)
- `id` (uuid, primary key)
- `clinic_id` (uuid, foreign key a profiles.id)
- `title` (text)
- `specialty_required` (text)
- `date_time` (timestamp/text)
- `duration_hours` (integer)
- `price` (numeric/integer)
- `status` (text: 'open', 'filled', 'completed')
- `shift_category` (text) - 'Guardia', 'Consultorio', 'Ambulancia'
- `viaticos` (text)
- `payment_timeframe` (text)
- `description` (text)
- `professional_id` (uuid, foreign key a profiles.id)
- `created_at` (timestamp)

### 3. Tabla: `shift_applications` (Postulaciones)
- `id` (uuid, primary key)
- `shift_id` (uuid, foreign key a shifts.id)
- `professional_id` (uuid, foreign key a profiles.id)
- `status` (text: 'pending', 'accepted', 'rejected')
- `created_at` (timestamp)

### 4. Tabla: `reviews` (Reseñas)
- `id` (uuid, primary key)
- `shift_id` (uuid, foreign key a shifts.id)
- `reviewer_id` (uuid, foreign key a profiles.id)
- `reviewed_id` (uuid, foreign key a profiles.id)
- `rating` (integer)
- `comment` (text)
- `created_at` (timestamp)

### 5. Tabla: `notifications` (Notificaciones)
- `id` (uuid, primary key)
- `user_id` (uuid, foreign key a profiles.id)
- `shift_id` (uuid, foreign key a shifts.id)
- `title` (text)
- `message` (text)
- `is_read` (boolean)
- `created_at` (timestamp)

## REGLAS DE DISEÑO
- Usa Tailwind CSS.
- Mantiene el estilo "Glassmorphism" con colores `bg-slate-50`, texto `slate-900` y detalles en `blue-600`.
- Usa bordes redondeados suaves (`rounded-xl` o `rounded-2xl`).
- Las actualizaciones a Supabase deben coincidir con estos nombres exactos.